# 快単 英単語記憶アプリ　画面遷移図（MVP / First stage）

|項目|内容|
|---|---|
|バージョン|v1.0（仕様確定フェーズ・ご確認用）|
|作成日|2026年5月22日|
|宛先|一般社団法人KAI　菊地雅文 様|
|関連文書|`01_快単_詳細仕様書_v1.0.md` / `03_快単_ER図_v1.0.md`|

> 本図は解説書の画面番号（①〜⑪）に準拠し、MVP（First stage）の画面遷移を示します。Second stage・育成キャラクター（⓪/⑫）は対象外のため**点線**で参考表示しています。
> 💡 図はMermaid記法で作成しています。GitHub・対応ビューアでは図として表示されます。

---

## 1. 全体遷移図（俯瞰）

```mermaid
flowchart TD
    Splash([起動 / スプラッシュ]) --> Start

    Start["① スタート画面<br/>First stage / Second stage<br/>各ステージに★5表示"]
    Start -->|First stage| Range["②③ 範囲指定画面<br/>46ブロック選択"]
    Start -.->|Second stage<br/>（MVP対象外）| Soon{{"「準備中」表示<br/>遷移なし"}}
    Soon -.-> Start

    Range -->|スタート / 初回OKを除く| Q
    Range -->|リセット| Range

    Q["⑥ 問題画面<br/>単語・品詞・№・TTS<br/>残/総数・OK・再チェック"]
    A["⑦ 解答画面<br/>意味・覚え方・例文<br/>OK / 再チェック"]

    Q -->|意味・例文| A
    A -->|OK / 再チェック<br/>（巡の途中）| Q

    A -->|当該巡の最終単語を判定| Result["⑧⑩ 結果表示画面<br/>総数・OK・再チェック"]

    Result -->|次に進む<br/>（再チェックあり）| Q
    Result -->|範囲再指定| Range
    Result -->|再チェック0＝全OK| End

    End["⑪ 指定範囲終了画面<br/>お祝い演出 3〜5秒"]
    End -->|演出後 自動| Start

    Start -.->|将来| Char(["⓪ キャラ選択画面<br/>（Second stage）"])
    Char -.-> Start

    classDef out stroke-dasharray: 4 4,fill:#f7f7f7;
    class Soon,Char out;
```

---

## 2. 学習セッションの状態遷移（満点法ロジック）

問題⇄解答の往復と巡目進行を、ステートマシンとして表現します。

```mermaid
stateDiagram-v2
    [*] --> 範囲指定
    範囲指定 --> セッション開始 : スタート / 初回OKを除く

    state セッション開始 {
        [*] --> 出題リスト構築
        出題リスト構築 --> ランダム並べ替え
        ランダム並べ替え --> [*]
    }

    セッション開始 --> 問題提示 : 巡目=1

    問題提示 --> 解答確認 : 「意味・例文」
    解答確認 --> 単語OK : 「OK」
    解答確認 --> 単語再チェック : 「再チェック」

    単語OK --> 巡内判定 : リストから除外
    単語再チェック --> 巡内判定 : 次巡リストへ追加

    巡内判定 --> 問題提示 : 当該巡に未出題あり
    巡内判定 --> 結果表示 : 当該巡の全単語を出題済み

    結果表示 --> 問題提示 : 次に進む（再チェックあり・巡目+1で再構成）
    結果表示 --> 範囲指定 : 範囲再指定
    結果表示 --> 指定範囲終了 : 全OK（再チェックなし）

    指定範囲終了 --> スタート画面 : 演出後・自動（完了ブロック薄色化／回転数+1）
    スタート画面 --> [*]
```

---

## 3. 範囲指定画面のブロック状態遷移

```mermaid
stateDiagram-v2
    [*] --> 未学習_白
    未学習_白 --> 選択中_青 : タップ / 一括選択
    選択中_青 --> 未学習_白 : 再タップ（選択解除）
    選択中_青 --> 完了_薄色 : 当該ブロックの全単語OK
    完了_薄色 --> 選択中_青 : タップ（再選択して再学習可）
    完了_薄色 --> 未学習_白 : リセット
    選択中_青 --> 未学習_白 : リセット

    note right of 完了_薄色
        完了後も「初回OKを除く」用に
        1巡目の再チェック記録を保持
    end note
```

---

## 4. 画面遷移一覧（表）

|遷移元|操作・条件|遷移先|
|---|---|---|
|起動|スプラッシュ後|① スタート画面|
|① スタート|First stage|②③ 範囲指定|
|① スタート|Second stage（MVP）|「準備中」表示（遷移なし）|
|②③ 範囲指定|ブロックタップ|（選択状態変化・同画面）|
|②③ 範囲指定|リセット|（白に戻す・同画面）|
|②③ 範囲指定|スタート／初回OKを除く|⑥ 問題画面|
|⑥ 問題画面|意味・例文|⑦ 解答画面|
|⑥ 問題画面|スピーカー|（TTS再生・同画面）|
|⑦ 解答画面|OK／再チェック（巡の途中）|⑥ 問題画面（次の単語）|
|⑦ 解答画面|OK／再チェック（巡の最終単語）|⑧⑩ 結果表示画面|
|⑧⑩ 結果表示|次に進む（再チェックあり）|⑥ 問題画面（次巡）|
|⑧⑩ 結果表示|範囲再指定|②③ 範囲指定画面|
|⑧⑩ 結果表示|再チェック0（全OK）|⑪ 指定範囲終了画面|
|⑪ 指定範囲終了|演出後（自動）|① スタート画面|

---

## 5. 注記

- ⑥問題画面 ⇄ ⑦解答画面 の往復は、操作速度を最優先するため**遷移アニメーションを実質無し（100ms以下）**とする。実装上は1画面内での表示切替とし、Navigatorの遷移負荷を排除する。
- ⓪キャラ選択画面・⑫Second stageは本MVPの**対象外**（点線表記）。将来同一フレームワーク上で追加する。
- 46ブロック全完了時も①スタート画面へ戻り、星評価が1回転分進む（詳細は仕様書「8. 星評価」）。

---

## 6. 変更履歴

|版|日付|内容|
|---|---|---|
|v1.0|2026-05-22|初版（仕様確定フェーズ・ご確認用）|
